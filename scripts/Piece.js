class Piece {
    constructor(x, y, team, myGame){
        this.x = x;
        this.y = y;
        this.team = team;
        this.myGame = myGame;
    }
    
    validMove(x,y) {
        //check to see if move is legal
    }
}